
class ezOsc
{
private:
    /* data */
public:
    bool feed(const char* data, size_t size);

    ezOsc(/* args */);
    ~ezOsc();
};

#if 0

address{
    const char* address;
    address * parent;
    address * first_child;
    address * next_sibling;
}

#endif